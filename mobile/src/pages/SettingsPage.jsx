import React from 'react'; import { Text, StyleSheet, TouchableOpacity } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context'; import { useNavigation } from '@react-navigation/native'; export default function SettingsPage() { const nav = useNavigation(); return <SafeAreaView style={styles.container}><Text style={styles.text}>Settings Page (WIP)</Text><TouchableOpacity onPress={() => nav.goBack()}><Text style={styles.btn}>Back</Text></TouchableOpacity></SafeAreaView>; } const styles = StyleSheet.create({ container: { flex: 1, backgroundColor: '#090D1A', alignItems: 'center', justifyContent: 'center' }, text: { color: '#FFF', fontSize: 18, marginBottom: 20 }, btn: { color: '#6366F1' } });
